Explain the difference between the = 
symbol in Java and in mathematics?

The = symbol in Java assigns values to variables, but does'nt
check if 2 values are the same (the == symbol does). In math, the
= symbol means that the two expressions are equal/the same.
