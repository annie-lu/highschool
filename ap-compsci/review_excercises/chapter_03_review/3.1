Explain the difference between a local variable and a parameter variable?
Parameter variables are initialized with the values that are supplied in the method called. 
Local variables are in the method but not initialized by the client class.