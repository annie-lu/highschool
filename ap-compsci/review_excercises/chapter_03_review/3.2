Explain the difference between an instance field and a local variable
Local variables belong to a method and once the method exits, it is gone.
You must initialize a local variable to use it, but you do not need to initialize 
a instance field to use it (they have default values).
