Explain what each of the following two program segments computes
x=2;
y=x+x;

so then y=4;
because x is a number
and the + operator adds the two numbers together

and

s="2";
t=s+s;

so then t="22";
because s is a string
and the + sign performs string concatenation